# =============================================================================
# Backend environment variables — copy to .env.local and fill in real values.
# =============================================================================

# -----------------------------------------------------------------------------
# LiveKit (real-time transport)
# Get these from https://cloud.livekit.io/ → your project → Settings
# -----------------------------------------------------------------------------
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key_here
LIVEKIT_API_SECRET=your_livekit_api_secret_here

# -----------------------------------------------------------------------------
# Murf (TTS — Text-to-Speech)
# Get your API key from https://murf.ai/api/dashboard
# -----------------------------------------------------------------------------
MURF_API_KEY=your_murf_api_key_here

# -----------------------------------------------------------------------------
# Deepgram (STT — Speech-to-Text)
# Get your API key from https://deepgram.com
# -----------------------------------------------------------------------------
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# -----------------------------------------------------------------------------
# LLM — use one of the following depending on which provider you use
# -----------------------------------------------------------------------------
# For Gemini (default): get from https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_google_api_key_here

# Twilio calls are initiated by the frontend server route. Keep these values in
# frontend/.env.local; they are listed here to document the shared deployment config.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_TO_NUMBER=
PUBLIC_BASE_URL=
LIVEKIT_SIP_URI=
LIVEKIT_SIP_OUTBOUND_TRUNK_ID=

# Local Commerce outbound-call context
ORDER_CUSTOMER_NAME=Shivam
ORDER_ID=ORD-1001
ORDER_ITEMS=1 x Amul Taaza Milk, 1 litre pouch, INR 68, seller Sharma General Store|1 x Britannia Brown Bread, 400 gram loaf, INR 45, seller Sharma General Store|1 x Homemade Mango Pickle, 500 gram jar, INR 240, seller Asha Foods
ORDER_TOTAL_INR=353
ORDER_DELIVERY_TIME=Today, 6 PM-8 PM

# -----------------------------------------------------------------------------
# Local catalogue API (separate service; default shown below)
# -----------------------------------------------------------------------------
CATALOGUE_API_URL=http://127.0.0.1:8001/catalogue

# For OpenAI: get from https://platform.openai.com/api-keys (if using OpenAI LLM)
# OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------------------------------------------------------
# Telephony (optional — only needed for src/telephony/)
# See src/telephony/README.md for the full setup.
# -----------------------------------------------------------------------------
# Required for outbound calls. Create the trunk with:
#   lk sip outbound create src/telephony/outbound/outbound-trunk.json
# LIVEKIT_SIP_OUTBOUND_TRUNK_ID=ST_your_trunk_id

# Optional — where the transfer_to_human tool sends callers.
# TRANSFER_TO_NUMBER=+15551234567
